import React from 'react';
import { View, Text } from 'react-native';

const SplashScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center' }}>
      <Text style={{ marginRight: 'auto', marginLeft: 'auto' }}>
        RAFI PEER SplashScreen
      </Text>
    </View>
  );
};

export default SplashScreen;
